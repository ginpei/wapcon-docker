{
  "name": "wapcon-docker",
  "private": true,
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "lint": "eslint ./index.js ./cli.js lib test",
    "start": "npm run test",
    "run-test": "NODE_ENV=test mocha test/",
    "test": "npm run lint -s && npm run run-test -s"
  },
  "dependencies": {
    "prompt": "^1.0.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-as-promised": "^7.1.1",
    "cross-env": "^5.1.3",
    "eslint": "^6.0.0",
    "mocha": "^6.0.0",
    "sinon": "^7.2.4"
  },
  "engines": {
    "node": ">=9"
  }
}
